<h1><%= @user.name %> messages</h1>
<div class="row">
  <div class="col-md-10">
    <table class="table table-striped">
       <thead calss="thread-light">
          <tr>
            <th>Title</th>
            <th>Message</th>
          </tr>
        </thead>
        <tbody>
          <% @messages.each do |message| %>
            <tr>
              <td><%= message.title %></td>
              <td><%= message.content %></td>
            </tr>
          <% end %>
         </tbody>
    </table>  
  </div>
</div>